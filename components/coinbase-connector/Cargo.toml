[package]
name = "coinbase-connector"
version = "1.0.0"
edition = "2021"

[dependencies]
tokio = { version = "1.28", features = ["full"] }
tokio-tungstenite = { version = "0.19", features = ["native-tls"] }
serde_json = "1.0"
# Adicionado para derive macros
serde = { version = "1.0", features = ["derive"] } 
futures-util = "0.3"
tracing = "0.1"
tracing-subscriber = "0.3"
# Adicionado para integraÃ§Ã£o com Kafka
rdkafka = { version = "0.34", features = ["tokio", "cmake-build"] }
# Adicionado para serializaÃ§Ã£o em Protobuf (vamos simular o cÃ³digo gerado por agora)
prost = "0.11"
prost-types = "0.11"
dotenv = "0.15.0"
chrono = { version = "0.4", features = ["clock"] }
http = "0.2"
thiserror = "1.0"
redis = { version = "0.24", features = ["aio", "tokio-comp", "connection-manager"] }
